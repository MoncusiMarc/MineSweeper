{
    "scripts": {
        "test:e2e": "cucumber-js --require cucumber.conf.js --require tests/steps/**/*.steps.js --format @cucumber/pretty-formatter --format html:tests/reports/cucumber-report.html --tags \"not @Manual\" --parallel 4"
    },
    "devDependencies": {
        "@cucumber/cucumber": "^8.6.0",
        "@cucumber/pretty-formatter": "^1.0.0",
        "@playwright/test": "^1.26.0",
        "playwright": "^1.26.0"
    }
}
